# Core (pinned to the versions used for the reported runs)
torch==2.2.0            # built against CUDA 12.1 (cu121 wheel)
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.2
nibabel==5.2.0          # NIfTI I/O for BraTS volumes
SimpleITK==2.3.1        # DICOM -> NIfTI + resampling (preprocessing/Preprocess_BRATS.py)
tqdm==4.66.1
matplotlib==3.8.2       # figures only

# Optional: fused selective-scan CUDA kernel for the SSM in pss_unet.py.
# If absent, pss_unet.py falls back to a correct pure-PyTorch scan (slower).
# Needs a CUDA build toolchain matching your torch/CUDA version.
# mamba-ssm==1.2.0.post1
# causal-conv1d==1.2.0.post2
